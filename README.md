# Public Update Artifacts

This folder contains files intended for a **public** update channel:

- `barcode_bridge.py` (published app script)
- `update-manifest.json` (version metadata + hash)

Publish only these artifacts to a public repo/CDN.
Keep your full source code in a private repository.
